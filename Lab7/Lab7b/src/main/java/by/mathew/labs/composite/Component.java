package by.mathew.labs.composite;

public interface Component {
    // Просто интерфейс, от которого имплементятся
    // Класс Composite (компоненты размером до лексемы включительно)
    // Класс Symbol (конечный компонент минимальной размерности)
}
